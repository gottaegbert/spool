# ~~OpenCLI Integration — Technical Design~~

> **DEPRECATED**: This system has been fully removed. All platform data fetching is now handled by the [Connector Architecture](./connector-sync-architecture.md). See that document for the current design.

> Original purpose: integrating OpenCLI to pull personal data from 50+ platforms via browser sessions. Replaced because each connector is now a standalone, installable plugin rather than a wrapper around a shared CLI tool.
